name: "Hit a webhook"
 
on:
  push:
    branches:
      - main
 
jobs:
  run-updater:
    runs-on: ubuntu-latest
    steps:
    - name: REST API with curl
      run: |
        curl -X GET "https://localhost:7019/api/User"
