name: Newman test

on:
  push:
    branches:
      - main

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
      - name: run tests
        run: |
          cd Tests && newman run apitesting.postman_collection.json
         
